package bitmanipulation

// Position of rightmost set bit
// right most set bit mask = x & x``

func SwapAdjBit(num uint8) uint8 {
	// tempNum := num
	// for i := 0; tempNum != 0; i++ {
	// 	x := num << 1
	// 	y := num >> 1
	// 	mask_x := (1 << i + 1)
	// 	mask_y := (1 << i)
		
	// }
	// return num
	return 1;
}
